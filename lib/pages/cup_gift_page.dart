import 'package:flutter/material.dart';

class CupGiftPage extends StatelessWidget {
  // const CupGiftPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Hero(tag:6,child: Container(child: Text("OK"),),);
  }
}
